server:
  port: 8081

spring:
  # 配置数据源
  datasource:
    url: jdbc:mysql:///bookshop?useUnicode=true&characterEncoding=utf-8
    username: root
    password: 123456
    driver-class-name: com.mysql.jdbc.Driver
  servlet:
    multipart:
      enabled: true
      max-request-size: 10MB
      max-file-size: 10MB

#upload:
#  path: E://imgs//

mybatis:
  type-aliases-package: com.book.entity


# 阿里云基本配置
aliyun:
  config:
    ossEndPoint: oss-cn-beijing.aliyuncs.com
    accessKeyId: 
    accessKeySecret: 
    bucketName: bookshop2
    filehost: book




